# Cheng Coffee V4
Bản tối ưu hiệu năng + bảo mật, giữ form/giao diện hiện tại nhưng chuyển backend sang Firestore.

Đọc `MIGRATION.md` trước khi deploy.

Các điểm chính:
- Firestore realtime
- Firebase Auth
- Cloud Functions tính giá phía server
- Security Rules deny-by-default
- App Check-ready
- FCM push không chặn việc đặt món
- chống đơn trùng
- Admin tự nhớ đăng nhập
